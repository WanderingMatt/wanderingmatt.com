<% unless @items.empty? %>
	<% for item in @items %>
		<%= render :partial => "item.html.erb", :locals => { :item => item, :offset => @offset, :item_unique_id => @total, :index => 20 } %>
		<% @total-=(1+item.grouped_items.length) %>
	<% end %>
<% end %>